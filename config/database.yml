development:
    adapter: postgresql
    encoding: utf8
    pool: 5
    username: postgres
    password: '12345'
    database: train_app
    host: localhost

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
    adapter:  postgresql
    encoding: utf8
    pool: 5
    username: postgres
    password: '12345'
    database: train_app_test
    host: localhost

production:
    adapter:  postgresql
    encoding: utf8
    pool: 5
    username: postgres
    password: '12345'
    database: train_app_production
    host: localhost